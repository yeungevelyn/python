class Node:
    """
    Represents a node in the data structure.
    -datum: the datum stored at the node in the data structure
    -next: reference to the next node in the data structure
    """
    def __init__(self, datum, next):
        self.datum = datum
        self.next = next




